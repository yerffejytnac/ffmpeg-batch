# FFmpeg Batch Processor Configuration
# Example .env for docker-compose
# Values here override compose defaults. Remove or change as needed.

# Container & runtime
CONTAINER_NAME=ffmpeg-batch-processor

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Host binding (IP on host), host port, and container port
# HOST_BIND can be an IP like 127.0.0.1 to restrict access to localhost.
HOST_BIND=0.0.0.0
HOST_PORT=8000

# Processing Configuration
MAX_WORKERS=4

# Paths (inside container)
INPUT_DIR=/data/input
OUTPUT_DIR=/data/output
LOGS_DIR=/data/logs

# Logging
LOG_LEVEL=INFO
